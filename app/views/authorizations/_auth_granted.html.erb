<%= form_for(authorization, remote: true, data:{type: :js}) do |f| %>
  <%= f.hidden_field :approval, value: "accepted" %>
  <%= f.submit "Grant Authorization", class: "btn btn-medium btn-success" %>
<% end %>
