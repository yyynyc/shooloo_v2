<%= form_for(user, :url => reset_student_password_authorization_path(user),
	remote: true, data:{type: :js}) do |f| %>
	<%= f.submit "Reset Password", class: "btn btn-small btn-primary", confirm: "Password for #{user.first_name} #{user.last_name} will be reset to shooloo. Continue?" %>
<% end %>