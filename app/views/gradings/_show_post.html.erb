<div class="bottom-margin normal-font">
	<p>
		<strong>I-can:</strong> 
		In <%= @grading.domain.name%>, 
		<% if  @grading.hstandard.nil? %>
			<%= @grading.standard.ICan %>
		<% else %>
			<%= @grading.hstandard.ICan %>
		<% end %>
	</p>  
</div>
<div class="well well-small margin-bottom">
	<table class="table-lesson">
		<tr>
			<th>Overall grade:</th>
			<td>
				<%= @grading.mark.full_mark %>
			</td>
		</tr>
		<tr>
			<th>Concept:</th>
			<td>
				Your post shows
				<% if @grading.concept == true %>
					<span class="yes">clearly understanding of</span> 
				<% else %>
					<span class="no">some confusion about</span>
				<% end %>
				the above I-can statement.
			</td>
		</tr>
		<tr>
			<th>Precision:</th>
			<td>
				Your post 
				<% if @grading.precision == true %>
					<span class="yes">defined crucial details</span>, e.g., units of measure, precisely and you <span class="yes">did not miss</span> any steps. 
				<% else %>
					<span class="no">did not define crucial details</span>, e.g., units of measure, precisely and/or you <span class="no">missed</span> some steps. 
				<% end %>.
			</td>
		</tr>
		<tr>
			<th>Computation:</th>
			<td>
				Your post's answer is 
				<% if @grading.computation == true %>
					<span class="yes">correctly calculated</span> 
				<% else %>
					<span class="no">incorrectly calculated</span>
				<% end %>.
			</td>
		</tr>
		<tr>
			<th>Grammer:</th>
			<td>
				Your post
				<% if @grading.grammar == true %>
					<span class="yes">has no </span> 
				<% else %>
					<span class="no">has some</span>
				<% end %>
				spelling and/or grammatical errors.
			</td>
		</tr>
		<tr>
			<th>Manners:</th>
			<td>
				Your post
				<% if @grading.courtesy == true %>
					<span class="yes">is friendly and helpful </span> 
				<% else %>
					<span class="no">needs to be more polite and helpful</span>
				<% end %>
				to others.
			</td>
		</tr>
		<tr>
			<th>Note:</th>
			<td>
				<% if @grading.note.nil? %>
					None.
				<% else %>
					<span class="chisel"><%= @grading.note.html_safe %></span>
				<% end %>
			</td>
		</tr>
	</table>
</div>
<%= render partial: 'posts/post_simple', locals: {post: @post} %>
<div class="center">
	<% if current_user?(@grading.grader) %>
		<%= link_to "Re-grade this post", edit_grading_path(@grading), class: "btn btn-large btn-primary" %>
	<% end %>
</div>