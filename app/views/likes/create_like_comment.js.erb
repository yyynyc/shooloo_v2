$("#like_form_comment_<%= @comment.id %>").html("<%= escape_javascript(
	render(partial:'likes/unlike_comment', 
		locals: {like: @like, comment: @comment})) %>")

$("#comment_likes_count-<%=@comment.id%>").html('<%= @comment.likes.count %>')
