$("#like_form_<%= @post.id %>").html("<%= escape_javascript(
	render(partial:'likes/like', locals: {like: @like, post: @post})) %>")

$("#post_likes_count-<%=@post.id%>").html('<%= @post.likes.count %>')

