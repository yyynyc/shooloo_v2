<% if activity.trackable %>	
	<span class='alert-heading'> 
		<strong><%= link_to "INVITED", new_post_comment_path(activity.trackable.post) %></strong> you to comment on post
		#<%= link_to activity.trackable.post.id, new_post_comment_path(activity.trackable.post) %>  
		on <%= activity.created_at.strftime("%b %d, %Y") %>.			
	</span>
	<i class='icon-envelope icon-medium'></i>	<br/>
<% else %>
	<span class='alert-heading'>
		sent you an invitation but has since withdrawn it.
	</span>	
	<i class='icon-trash'></i>
<% end %>