<% if activity.trackable %>
	<span class='alert-heading'>
		added a <strong><%= link_to "RATING", new_post_comment_path(activity.trackable.rated_post) %></strong> 
		on <%= activity.created_at.strftime("%b %d, %Y") %> to post
		(#<%= link_to activity.trackable.rated_post.id, new_post_comment_path(activity.trackable.rated_post) %>)
	</span>
	<br/>
	that you have published or rated.
	Check out the aggregated rating results to <%= link_to "this post", new_post_comment_path(activity.trackable.rated_post) %>. <br/>
	Check out 
	<%= link_to "#{activity.user.screen_name}'s posts", posts_user_path(activity.user) %> and add your rating.
<% else %>
	<span class='alert-heading'>
		added and then deleted a rating.
	</span>
<% end %> 