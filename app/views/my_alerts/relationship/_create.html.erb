<% if activity.trackable %>
	has started <strong>FOLLOWING</strong> you since 
	<%= activity.created_at.strftime("%b %d, %Y") %>. <br/>
	<strong><%= link_to "Publish a word problem", root_url %></strong> to impress <%= activity.user.screen_name %>. <br/>
	Express your thanks and 
	<%= link_to "follow #{activity.user.screen_name} back", posts_user_path(activity.user) %>.
<% else %>
	started following you but since unfollowed you.
<% end %>