<%= f.input :level_id, as: :select, include_blank: true, label: "Suitable grade:", collection: Hash[Level.all.map{|level| [level.name, level.id]}] %>
<%= f.input :domain_id, as: :select, include_blank: true, label: "Skills involved", collection: option_groups_from_collection_for_select(Level.order(:name), :domains, :name, :id, :name, f.object.domain_id) %>  
<%= f.input :standard_id, as: :select, include_blank: true, label: "CCSS Standard", collection: option_groups_from_collection_for_select(Domain.order(:name), :standards, :name, :id, :description, f.object.standard_id) %>
<%= f.input :hstandard_id, as: :select, include_blank: true, label: "High School Standard", collection: option_groups_from_collection_for_select(Standard.order(:description), :hstandards, :description, :id, :description, f.object.standard_id) %>